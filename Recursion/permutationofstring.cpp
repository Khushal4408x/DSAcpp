#include <iostream>
#include<vector>
using namespace std;
void permutstr(string s,vector<string>&ans,int i ){
if(i==s.size()){
    ans.push_back(s);
}
int n=s.size();
for(int idx=i;i<n;i++){


}
}
int main(){
    string s="ABC";
    vector<string>ans;
    permutstr(s,ans,0);
}